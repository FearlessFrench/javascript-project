// Math = built-in object that provides a
//        collection of properties and methods

/*
console.log(Math.PI);
console.log(Math.E); 

let x = 3.21;
let y = 2;
let z;

z = Math.round(x);
console.log(z);

z = Math.floor(x); // round down
z = Math.ceil(x); // round up
z = Math.trunc(x); // eliminate any decimal proportion
z = Math.pow(x, y); // x ** y
z = Math.sqrt(x); // x ** (1/2)
z = Math.log(x);
z = Math.sin(x);
z = Math.cos(x);
z = Math.tan(x);
z = Math.abs(x);
z = Math.sign(x); // return -1 or 0 or 1
*/

let x = 3;
let y = 2;
let z = 1;

let max = Math.max(x, y, z);
let min = Math.min(x, y, z);

console.log(max);
console.log(min);
